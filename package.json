{
  "name": "browser-control-marketplace",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Marketplace wrapper for the Browser Control Codex plugin.",
  "scripts": {
    "test": "npm --prefix plugins/browser-control test",
    "check:syntax": "npm --prefix plugins/browser-control run check:syntax",
    "check:runtime": "npm --prefix plugins/browser-control run check:runtime",
    "validate:paths": "npm --prefix plugins/browser-control run validate:paths",
    "validate": "npm --prefix plugins/browser-control run validate",
    "validate:runtime": "npm --prefix plugins/browser-control run validate:runtime"
  }
}
